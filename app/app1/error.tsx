'use client';
// @flow
import * as React from 'react';

type Props = {

};
const Error = (props: Props) => {
    return (
        <div>
            An error occurred.
        </div>
    );
};

export default Error;